# Ralph Progress Log
Started: (not yet run)
---
--- Slice 01 completed: Monorepo scaffold + shared data package ---
2026-04-01
- pnpm-workspace.yaml at root with packages/* glob
- Root package.json with pnpm workspaces, typecheck/test/build scripts delegating via pnpm -r
- Root tsconfig.json: strict mode, NodeNext module resolution, resolveJsonModule: true
- packages/data: @benchmark/data with Product, Category, CartItem, ApiResponse<T> types
- 100-product fixture across all 8 categories; image_url uses picsum.photos/seed/product-{id}/400/300
- Import attribute syntax: use `with { type: "json" }` not `assert { type: "json" }` (TypeScript 5.x)
- Pre-existing packages/ui-reference from slice 03 was already committed; it needed cart.html which was already present
- All 4 vitest tests pass: count=100, field conformance, 8 categories, picsum URL pattern
---
--- Slice 02 completed: Shared API server with latency simulation ---
2026-04-01
- packages/api: @benchmark/api Hono server on @hono/node-server, TypeScript
- Endpoints: GET /products (100 items), GET /products/:id, GET /categories (8), GET /health
- LATENCY_MS env var applies setTimeout delay to all data endpoints; /health is never delayed
- ApiResponse<T> uses the actual type from @benchmark/data (has `data` + optional `total`), not `success: boolean`
- /products/:id 404 response uses { success: false, error: "Not found" } shape
- createApp() exported for programmatic use in tests (no server startup side-effects during test collection)
- Server start guard uses process.argv[1] check; tests call app.fetch() directly via Hono's built-in fetch handler
- 10 integration tests pass: counts, shapes, 404 behaviour, latency >= 500ms, health <50ms under latency
- Root pnpm test was pre-existing-broken (packages/benchmark stub has no test files); api tests pass directly
---
--- Slice 11 completed: astro-solid — Astro with SolidJS islands, all four pages ---
2026-04-01
- Static output (output: 'static') with @astrojs/solid-js integration and @tailwindcss/vite plugin
- passthroughImageService() is REQUIRED — the noop service entrypoint still causes Astro to fetch remote images at build time; passthroughImageService() is the only way to skip remote image fetching entirely
- picsum.photos uses HTTP redirects; Astro's loadRemoteImage rejects redirects — passthroughImageService avoids this entirely
- All data comes from @benchmark/data static import (no API fetch at build time — API is not running during Docker build)
- SolidJS islands (client:load): FilterPage.tsx uses createSignal/createMemo/For/Show for filtering; CartPage.tsx uses createStore/produce for cart state
- Do NOT pass import.meta.env values as props to client:load islands — esbuild processes the .astro file for client bundling and Unexpected "env" errors occur in Docker (esbuild@0.25.12) even though local build succeeds
- Dockerfile must copy root tsconfig.json (line: COPY pnpm-workspace.yaml package.json pnpm-lock.yaml tsconfig.json ./) or data package build fails in Docker
- Static site served with `serve` package on port 3000; docker-compose maps 3006:3000
- 103 pages built: 100 product detail pages + index + filter + cart
---
--- Slice 10 completed: astro-vanilla — Astro with zero client-side framework, all four pages ---
2026-04-01
- Static output (output: 'static') with @tailwindcss/vite plugin
- passthroughImageService() required (same as astro-solid) — picsum.photos URLs redirect and Astro's default image pipeline rejects them
- All data from @benchmark/data static import — API not available at Docker build time
- Listing page: SSG with static products grid; Product detail: getStaticPaths() from static data
- Filter page: vanilla JS <script define:vars={{ productsJson, minPrice, maxPrice }}> for client-side filtering
- Cart page: vanilla JS localStorage-based cart with inline <script> TypeScript
- Static site served with `serve` on port 3000; docker-compose maps 3005:3000
- 103 pages built: 100 product detail + index + filter + cart
---
--- Slice 07 completed: nextjs-pages — Next.js Pages Router, all four pages ---
2026-04-01
- Next.js Pages Router with output: 'standalone' in next.config.ts
- Listing page: getStaticProps (SSG) from @benchmark/data
- Product detail: getStaticPaths + getStaticProps from @benchmark/data
- Filter page: CSR with useState/useMemo; product data passed as initialProducts prop from getStaticProps
- Cart page: CSR with useState; localStorage-based cart
- next/image used for product images
- Docker: multi-stage build, copies .next/standalone + .next/static + public; node server.js
- docker-compose maps 3002:3000 with API_URL env var
---
--- Slice 09 completed: nuxt — Nuxt 3, all four pages ---
2026-04-01
- Nuxt 3 with @nuxt/image, nitro node-server preset
- Listing + detail pages: prerender via defineRouteRules; products from @benchmark/data static import
- Filter + cart pages: definePageMeta({ ssr: false }) for CSR; Vue refs and computed
- @tailwindcss/vite plugin in nuxt.config.ts vite.plugins
- Docker: multi-stage, node .output/server/index.mjs; port 3000
- docker-compose maps 3004:3000, NUXT_PUBLIC_API_BASE=http://api:3000
---
--- Slice 12 completed: qwik — QwikCity, all four pages ---
2026-04-01
- QwikCity with express adapter for Node; static data from @benchmark/data
- Listing + detail pages: SSR via routeLoader$ or direct import of static products
- Filter + cart pages: CSR with useSignal/useStore
- @tailwindcss/vite in vite.config.ts
- Docker: multi-stage build; node dist/server/entry.express or equivalent
- docker-compose maps 3007:3000
---
--- Slice 13 completed: solidstart — SolidStart, all four pages ---
2026-04-01
- SolidStart with vinxi/nitro node-server preset
- Listing + detail pages: static data from @benchmark/data, SSR via createAsync/cache
- Filter + cart pages: CSR with createSignal/createStore
- @tailwindcss/vite in app.config.ts vite config
- Docker: multi-stage build; node .output/server/index.mjs
- docker-compose maps 3008:3000
---
2026-04-01
- SvelteKit 2.x with @sveltejs/adapter-node; vitePreprocess must be imported from @sveltejs/vite-plugin-svelte (NOT @sveltejs/kit/vite)
- tsconfig.json extends .svelte-kit/tsconfig.json (auto-generated); root strict options layered on top
- SSR pages (+page.server.ts): listing (/), product detail (/products/[id]) — fetch from process.env['API_URL'] at request time
- CSR pages: /filter and /cart use Svelte writable stores; browser fetches from /api/products (SvelteKit proxy route)
- Proxy route src/routes/api/products/+server.ts forwards to API_URL server-side; prevents browser contacting internal Docker API URL
- Dockerfile builder copies root tsconfig.json; runner preserves full workspace structure for @benchmark/data workspace dep resolution
- Test with docker compose (not standalone docker run) so sveltekit container reaches api:3000 on benchmark-net
- host.docker.internal:3000 does NOT reliably point to the Node API on macOS — VS Code Helper grabbed that port
- docker-compose.yml sveltekit service: port 3003:3000 (container always exposes 3000), API_URL=http://api:3000
---
